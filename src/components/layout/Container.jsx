import React from 'react'

const Container = ({ children }) => {
    return (
        <section className='max-w-298.5 mx-auto w-full'>
            {children}
        </section>
    )
}

export default Container